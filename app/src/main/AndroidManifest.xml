<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lab.funwith.picasso" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <activity -->
        <!-- android:name=".softinputmode.InputModeSimpleShowCase" -->
        <!-- android:label="Soft input mode/InputModeSimpleShowCase"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN"/> -->
        <!-- <category android:name="lab.funwith.picasso.SAMPLE_CODE"/> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <activity
            android:name=".model.PicassoInRecyclerViewActivity"
            android:label="model/PicassoInRecyclerViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="lab.funwith.picasso.SAMPLE_CODE"/>
            </intent-filter>
        </activity>

    </application>

</manifest>
